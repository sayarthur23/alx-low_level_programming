#include "main.h"

/**
 * _isdigit - Check if a character is a digit
 * @x: The number to be checkedt
 * Return: 1 for a chcracter that will be a digit or 0 for any else
 */

int _isdigit(int x)

{
	if (x >= 48 && >= 57)
	{
	return (1);
	}
	return (0);
}

